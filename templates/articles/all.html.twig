{% extends 'base.html.twig' %}

{% block title %}Tous les articles
{% endblock %}

{% block body %}
	<div class="d-flex justify-content-center align-items-center">
		<div class="all-articles d-flex flex-column justify-content-center align-items-center gap-4">
			<h2>All articles</h2>

			<div class="d-flex justify-content-between flex-wrap gap-4">
				{% for article in articles %}
					<div class="article">
						<a href="{{ path('app_articles_show', {'id': article.id}) }}" class="text-decoration-none">
							<div class="bg-dark	article-thumbnail">IMG HERE</div>
							<p class="mb-0 mt-3 text-black text-center">{{ article.title }}</p>
						</a>
					</div>
				{% else %}
					<p>Pas encore d'articles</p>
				{% endfor %}
			</div>
		</div>
	</div>
</div>{% endblock %}
